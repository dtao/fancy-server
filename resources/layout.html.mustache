<!DOCTYPE html>
<html>

<head>
    <title>{{title}}</title>
    <link rel="stylesheet" type="text/css" href="{{themeHref}}" />
    <link rel="stylesheet" type="text/css" href="{{highlightThemeHref}}" />

    <style type="text/css">
      /*
       * Undo Bootstrap's padding and border-radius,
       * as they conflict with highlight.js
       */
      pre {
        padding: 0;
        border-radius: 0;
      }
    </style>
</head>

<body>
    <main class="container">
        {{> content}}
    </main>
</body>

</html>
